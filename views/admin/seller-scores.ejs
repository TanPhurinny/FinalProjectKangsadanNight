<!DOCTYPE html>
<html lang="th">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>คะแนนร้านค้า | Kangsadan Admin</title>
        <link rel="icon" type="image/png" href="/img/favicon.png">
        <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
        <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500;600;700&family=IBM+Plex+Mono:wght@400;500;600&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="/stylesheets/partials/navbar.css?v=20260724-6">
        <link rel="stylesheet" href="/stylesheets/admin/theme.css">
    </head>
    <body class="bg-light">
        <script src="/js/common/tabSession.js"></script>
        <%- include('../partials/navbar') %>

        <div class="container py-4">
            <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger mb-3"><%= error %></div>
            <% } %>
            <% if (typeof success !== 'undefined' && success) { %>
            <div class="alert alert-success mb-3">บันทึกสำเร็จ</div>
            <% } %>

            <h1 class="fw-bold mb-1"><i class="fas fa-star me-2"></i>คะแนนร้านค้า</h1>
            <p class="text-muted mb-4">คะแนนสะสม = ค่าเฉลี่ยคะแนนของทุกรอบที่ร้านนั้นเคยขาย ร้านที่คะแนนต่ำมากสามารถ Blacklist ได้ — บล็อกไม่ให้สมัคร/จองแผงรอบถัดไป</p>

            <%- include('../partials/seller-score-table') %>
        </div>
    </body>
</html>
