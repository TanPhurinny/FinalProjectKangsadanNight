<% 
    // ตรวจสอบหน้าปัจจุบันจาก URL path เพื่อไฮไลต์เมนูที่ใช้งานอยู่
    const currentPath = locals.path || ''; 
%>

<nav class="management-nav">
    <div class="nav-container">
        <a href="/admin/dashboard" class="nav-brand">
            <i class="fas fa-store-alt"></i> Kangsadan
        </a>

        <button class="nav-toggle" id="navToggle">
            <i class="fas fa-bars"></i>
        </button>

        <div class="nav-links" id="navMenu">
            <a href="/booking-status" class="nav-item <%= currentPath.includes('booking-status') ? 'active' : '' %>">
                <i class="fas fa-clipboard-list"></i> สถานะการจอง
            </a>
            <a href="/select-zone" class="nav-item <%= currentPath.includes('select-zone') ? 'active' : '' %>">
                <i class="fas fa-store"></i> จองแผง
            </a>
            <a href="/repair" class="nav-item <%= currentPath.includes('repair') ? 'active' : '' %>">
                <i class="fas fa-tools"></i> แจ้งซ่อม
            </a>
            <a href="/notifications" class="nav-item <%= currentPath.includes('notifications') ? 'active' : '' %>">
                <i class="fas fa-bell"></i> แจ้งเตือน
            </a>

            <div class="nav-actions d-flex align-items-center">
                <div class="user-badge shadow-sm">
                    <i class="fas fa-user-circle"></i>
                    <%= user ? user.name : 'ผู้ใช้งาน' %>
                </div>
                <a href="/logout" class="logout-link">
                    <i class="fas fa-sign-out-alt me-1"></i>ออกจากระบบ
                </a>
            </div>
        </div>
    </div>
</nav>

<script>
    const navToggle = document.getElementById('navToggle');
    const navMenu = document.getElementById('navMenu');

    navToggle.addEventListener('click', () => {
        navMenu.classList.toggle('active');
        const icon = navToggle.querySelector('i');
        icon.classList.toggle('fa-bars');
        icon.classList.toggle('fa-times');
    });
</script>